/* const express = require('express');
const router = express.Router();

app.use("/id", function (req, res, next){
    req.headers = {
        packageName: packageName
    };
    req.fetch = fetch;
    next();
}, idRouter);

router.use("/model_code", function (req, res, next) {
    req.headers = {
        packageName: packageName
    };
    req.fetch = fetch;
    next();
}, modelCodeRouter);

module.exports = router; */